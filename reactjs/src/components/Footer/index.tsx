import React, { Component } from 'react';

import styles from './index.scss';

export default class index extends Component {
  render() {
    return <footer className={styles['poi-footer']}/>;
  }
}
