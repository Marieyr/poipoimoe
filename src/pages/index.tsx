import React from 'react';
import styles from './index.scss';

export default function() {
  return (
    <section>
      <section className={styles['poi-carousel']}></section>
      <section className={styles['poi-body']}></section>
    </section>
  );
}
